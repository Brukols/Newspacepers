import 'package:flutter/material.dart';

class NewsRssRoute extends StatelessWidget {

    @override
    Widget build(BuildContext context) {
        return Scaffold(
            appBar: AppBar(
                title: Text("Article"),
            ),
            body: ListView(
                children: <Widget>[],
            )
        );
    }
}